let bulan = ['Januari', 'februari', 'Maret', 'April'];
let shifted = bulan.shift();
console.log(bulan)
console.log(shifted);

/*NOTE:
    Array shift adalah fungsi untuk menghapus elemen pertama dari sebuah array.
    sama kaya arrayPop. hanya saja fungsi shift digunakan untuk menghapus elemen pertama
    */